#ifndef __ENUM__
    #define __ENUM__

    enum class Mode
    {
        solo,
        duo
    };

    enum class PlayerStatus
    {
        ongoing,
        check,
        checkmate,
        stalemate,
        draw
    };

#endif