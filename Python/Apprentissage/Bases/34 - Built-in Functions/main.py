"""
abs(n) : renvoie la valeur absolue de 'n'
round(n) : arrondi la décimal de 'n'
max(list) : renvoie l'élément max d'une liste
min(list) : renvoie l'élément min d'une liste
sum(list) : renvoie la somme de tous les éléments de la liste
all(list) : si tous les éléments sont égal à True, renvoie True
any(list) : si au moins un éléments est égal à True, renvoie True

Conversions de type de variable : 
type(var) : renvoie le type de la variable 'var'
str(n) : convertie 'n' en string
int(n) : convertie 'n' en int
float(n) : convertie 'n' en décimal

tuple(liste) : convertie la 'liste' en tuple
list(liste) : convertie 'liste' en liste
list(dico.keys) : convertie les clés du dictionnaire en liste

bin(n) : convertie 'n' en binaire
hex(n) : convertie 'n' en hexadécimal
oct(n) : convertie 'n' en octet
bytes(n) : convertie 'n' en bit
"""